# Discovery fixtures

These fixtures contain sanitized command output.

- `wd19-ioreg.txt` models macOS IORegistry topology.
- `wd19-lsusb.txt` models Linux `lsusb` output.
- The fixtures contain no firmware payloads.
- The parser tests do not access hardware.
